import { Button } from "@material-tailwind/react";
import React from "react";

const ButtonComponent = ({ color }) => {
  return <Button color={color}>Button</Button>;
};

export default ButtonComponent;
